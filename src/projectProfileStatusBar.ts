import * as vscode from 'vscode';

/**
 * Заглушка: статус-бар больше не используется после удаления “активного профиля”.
 */
export function refreshProjectStatusBar(_item: vscode.StatusBarItem): void {
    // no-op
}
